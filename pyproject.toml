[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cybershell"
version = "2.0.0"
description = "CyberShell RPG v2.0 - A Terminal-Based Cyber Adventure and Linux Shell Game"
readme = "README.md"
dependencies = ["textual>=8.2.8,<9"]
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [
    { name = "Amy", email = "amy@cybershell.dev" },
    { name = "CyberShell Dev Team" }
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Operating System :: POSIX :: Linux",
    "Operating System :: MacOS",
    "Operating System :: Microsoft :: Windows",
    "Topic :: Games/Entertainment",
    "Environment :: Console",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4.0",
    "pytest-cov>=4.1.0",
    "ruff>=0.1.0",
]

[project.scripts]
cybershell = "cybershell.run:main"

[tool.setuptools.packages.find]
where = ["src"]
exclude = ["cybershell.tests*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
addopts = "-v"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E402", "E501", "F401", "F541", "F841", "I001", "W291", "W292", "W293"]
